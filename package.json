{
  "name": "qeses",
  "version": "1.0.0",
  "description": "Qeses is a CLI that creates a simple static server, for serving static files on localhost, written in Node and Typescript, powered by ESbuild. It is blazingly fast, does not abstract anything and is customisable to the extreme.",
  "main": "bin/index.js",
  "bin": {
    "qeses": "./bin/index.js"
  },
  "scripts": {
    "compile": "esbuild src/index.mts --bundle --minify --platform=node --target=node18 --outdir='bin'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RuFuRu/Qeses.git"
  },
  "keywords": [
    "quick-static-server",
    "node",
    "typescript",
    "esbuild"
  ],
  "author": "RuFuRu",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^20.3.1",
    "chalk": "^5.2.0",
    "esbuild": "^0.18.4"
  }
}
